root = "."
tmp_dir = "tmp"

[build]
  bin = "bin/main"
  cmd = "go build -o bin/main cmd/main.go"
  include = ["*.go"]
  exclude = ["bin" ]

[color]
  main = "yellow"
  watcher = "cyan"
  build = "green"
  log = "white"
